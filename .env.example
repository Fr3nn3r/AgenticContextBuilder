# =============================================================================
# LLM API Configuration
# =============================================================================
# The system supports both Azure OpenAI and standard OpenAI. If Azure OpenAI
# credentials are configured, they will be used automatically. Otherwise,
# standard OpenAI credentials are used as fallback.

# Azure OpenAI (preferred for enterprise deployments)
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_BASE_URL=https://your-resource.openai.azure.com/openai/v1/
AZURE_OPENAI_DEPLOYMENT=gpt-4o
# AZURE_OPENAI_API_VERSION=2024-02-15-preview  # Optional, defaults to 2024-02-15-preview

# Standard OpenAI (fallback if Azure not configured)
OPENAI_API_KEY=your-openai-api-key
# OPENAI_MODEL=gpt-4o-mini  # Optional, defaults to gpt-4o-mini

# =============================================================================
# Azure Document Intelligence (for PDF extraction)
# =============================================================================
AZURE_DI_API_KEY=your-azure-di-api-key
AZURE_DI_ENDPOINT=https://your-resource.cognitiveservices.azure.com/

# =============================================================================
# Optional: Tesseract OCR Configuration (for Windows)
# =============================================================================
# If Tesseract is not in PATH, specify the full path to tesseract.exe
# Common Windows installation paths:
# - C:\Program Files\Tesseract-OCR\tesseract.exe
# - C:\Program Files (x86)\Tesseract-OCR\tesseract.exe
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
